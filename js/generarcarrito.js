

for (let i = 0; i<objetos_recup.length;i++) {
    let 
    let objeto_recup = JSON.parse(localStorage.getItem("objeto"));
}

//  seleccionar con un for cada propiedad para appendear a ul como li

for (let i = 0; i<objetos_recup.length;i++) {
    let cod_r = objetos_recup[i].codigo;
    let name_r = 
    let descr_r =
    let cantidad_r = 
    le
}